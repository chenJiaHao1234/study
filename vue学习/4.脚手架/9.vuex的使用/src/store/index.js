import Vue from "vue";
import Vuex from 'vuex';

Vue.use(Vuex)

const store = new Vuex.Store({
    // actions 用于相应组件中的动作，处理异步操作
    actions: {
        
    },

    // mutations 用于操作数据(state)
    mutations: {
        
    },

    // state 用于存储数据
    state: {
        
    },

    // getters 类似于计算属性 通过已有的数据计算出新的数据
    getters: {
        
    },

    modules: {
        
    }
})

export default store